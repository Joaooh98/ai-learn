# Real provider key. Used only by LiteLLM Proxy.
OPENAI_API_KEY=sk-your-openai-api-key

# Local proxy key. Used by the application to authenticate against LiteLLM Proxy.
LITELLM_MASTER_KEY=sk-local-proxy-key
